# Supabase RLS Auth Demo

Next.js + Supabase membership-club demo. Work in progress.

More to come:

- Postgres schema + Row-Level Security policies
- Supabase auth (magic link) + role-gated middleware
- Member/admin pages
- Integration tests proving the RLS policies actually work
- CI

See later commits/branches for the rest.
